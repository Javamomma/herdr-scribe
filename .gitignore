# Local config / secrets — never commit the real ones (ship .example templates)
scribe.conf
glossary.txt

# Python
__pycache__/
*.pyc
.pytest_cache/

# Build artifacts (loopback exe, etc.)
*.exe
*.dll

# Editor / OS
.DS_Store

# Default output tree (transcripts, notes, sidecars, drafts, audit log)
meetings/

# Local tooling
.venv/
